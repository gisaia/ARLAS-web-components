import { ProtectImageDirective } from './protect-image.directive';

describe('ProtectImageDirective', () => {
  it('should create an instance', () => {
    const directive = new ProtectImageDirective();
    expect(directive).toBeTruthy();
  });
});
